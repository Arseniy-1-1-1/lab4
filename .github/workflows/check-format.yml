name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  success:
    runs-on: ubuntu-latest
    steps:
    - name: Success
      run: echo "CI passed"